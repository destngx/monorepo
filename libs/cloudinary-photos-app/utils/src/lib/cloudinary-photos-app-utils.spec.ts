import { cloudinaryPhotosAppUtils } from './cloudinary-photos-app-utils';

describe('cloudinaryPhotosAppUtils', () => {
  it('should work', () => {
    expect(cloudinaryPhotosAppUtils()).toEqual('cloudinary-photos-app-utils');
  });
});
