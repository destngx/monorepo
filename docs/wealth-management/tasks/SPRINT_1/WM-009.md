**Task ID**: WM-009
**Title**: [Infrastructure] MCP Server Core implementation (Go)
**Status**: DONE
**Reporter**: PM
**Assignee**: Dev Lead
**Priority**: High
**Related Docs**: [\_technical/2-AI-Systems/Orchestration_and_Tools.md](file:///Users/ez2/projects/personal/monorepo/docs/wealth-management/_technical/2-AI-Systems/Orchestration_and_Tools.md)
**Description**:
Implement the Model Context Protocol (MCP) server foundation in the **Engine**.

- **Tools**: Use the `mcp-sdk-go` (or standard JSON-RPC if SDK is TBD).
- **Goal**: Expose an initial `EngineHealth` tool via MCP.
  **Acceptance Criteria**:
- Engine startup initializes an MCP JSON-RPC session over stdio.
- AI (e.g., Opencode) can successfully list and call the `EngineHealth` tool.

**Implementation Notes**:

- **MCP Transport**:
  - Implemented stdio JSON-RPC mode in `adapter/mcp/server.go`.
  - `main.go` now starts MCP server automatically with HTTP server startup.
- **Core Methods**:
  - `initialize`
  - `tools/list` (returns `EngineHealth`)
  - `tools/call` (supports `EngineHealth`)
- **Initial Tool**:
  - `EngineHealth` returns health payload with `status` and UTC `timestamp`.
- **TDD/BDD Tests**:
  - Added BDD-style tests in `adapter/mcp/server_test.go` validating:
    - Initialization response
    - Tool listing response
    - `EngineHealth` tool-call response

**Testing**:

- Unit/integration suite: `bunx nx run wealth-management-engine:test` passes.
- CLI stdio JSON-RPC session verified with:
  - `go run .`
  - `initialize` → success
  - `tools/list` → lists `EngineHealth`
  - `tools/call` (`EngineHealth`) → returns health content

---
